# Algorithm parameters  NI = 4           # Number of Islands  NM = 5           # Number of Algorithms in each Island  MM = master      # Migration Strategy: none, ring, fully, master  R  = 10          # Best solutions migrates at every N*R iterations# Metaheuristic parameters  MC = greedyB2    # Construction: random, equal, balanced, greedy1, greedy2 (A, B, C)  AG = 0.5         # Alpha coefficient controlling the greedyness: 0-purely random, 1-purely greedy  ME = ext_fhi     # Exploration: exc, ins, ext  MS = bvns        # Search: grasp, ils, vnd, bvns, gvns, sa# VNS specific parameters  V1 = 2           # Shake procedure  for VNS (Basic or General)  B1 = 0.1         # Beta coefficient for VNS (Basic or General)